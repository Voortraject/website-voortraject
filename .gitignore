# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files (secrets) — keep the example template
.env
.env.*
!.env.example

# MCP config — contains a local access token, must stay out of git
.mcp.json

# Supabase CLI local state (project ref, pooler URL) — not source
supabase/.temp/

# Original photo material (HEIC) — optimized versions live in src/assets
voortraject-fotos/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
